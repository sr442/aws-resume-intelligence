name: Deploy to AWS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  id-token: write # This is required for requesting the JWT
  contents: read  # This is required for actions/checkout

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: infrastructure/package-lock.json

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          # You need to create this Role in AWS with trust policy for this repo
          # Or use access keys (less secure) stored in GitHub Secrets
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Check for Secrets
        if: ${{ failure() }}
        run: |
          if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then echo "AWS_ACCESS_KEY_ID is missing or empty"; else echo "AWS_ACCESS_KEY_ID is present"; fi
          if [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then echo "AWS_SECRET_ACCESS_KEY is missing or empty"; else echo "AWS_SECRET_ACCESS_KEY is present"; fi

      - name: Install Dependencies (Infrastructure)
        run: |
          cd infrastructure
          npm ci

      - name: Install CDK
        run: npm install -g aws-cdk

      - name: Deploy Stack
        run: |
          cd infrastructure
          npx cdk deploy --require-approval never
